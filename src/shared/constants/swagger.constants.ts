/**
 * Swagger Authentication Constants
 * Constantes de Autenticación para Swagger
 */
export const SWAGGER_AUTH = {
  JWT_BEARER: 'JWT-auth',
  API_KEY: 'api-key',
} as const

/**
 * Swagger API Tags Constants
 * Constantes de Tags de API para Swagger
 */
export const SWAGGER_TAGS = {
  AUTH: 'auth',
  USERS: 'users',
  HEALTH: 'health',
} as const
